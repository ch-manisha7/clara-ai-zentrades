# Clara AI Zentrades -- Environment Variables
# Copy this file to .env and fill in your values

# Optional: Anthropic API key for LLM-based extraction (much better accuracy)
# Without this, the pipeline uses rule-based extraction (~85% completeness)
# With this, the pipeline uses Claude API extraction (~95% completeness)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
